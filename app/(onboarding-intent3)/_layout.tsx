import { Stack } from "expo-router";

export default function OnboardingIntent3Layout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
        animation: "fade",
      }}
    />
  );
}
