import { Stack } from "expo-router";

export default function OnboardingIntent1Layout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
        animation: "fade",
      }}
    />
  );
}
