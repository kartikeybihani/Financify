MANUALLY CREATED RPC FUNCTIONS
================================

ACTIVE FUNCTIONS (USED IN CODEBASE)
===================================

TRANSACTION & CATEGORY MANAGEMENT
---------------------------------

1. update_transaction_category_by_name ⭐ ACTIVE
   - Updates a single transaction's category by name
   - Parameters: p_transaction_id (uuid), p_new_category (text)
   - Returns: boolean
   - Purpose: Individual transaction category override using category names
   - Used in: CategorySelector.tsx (single transaction updates)

2. update_similar_transactions_by_name ⭐ ACTIVE
   - Updates all transactions with same merchant name and creates category rule
   - Parameters: p_merchant_name (text), p_new_category (text), p_user_id (uuid)
   - Returns: integer (updated_count)
   - Purpose: Bulk update similar transactions and create rules for future categorization
   - Used in: CategorySelector.tsx (similar transactions updates)

3. get_affected_transaction_ids ⭐ ACTIVE
   - Returns transaction IDs affected by category updates
   - Parameters: p_user_id (uuid), p_merchant_name (text), p_new_category (text)
   - Returns: TABLE(transaction_id uuid, transaction_name text, amount numeric, date date)
   - Purpose: Get details of transactions affected by category changes
   - Used in: CategorySelector.tsx (tracking affected transactions)

FINANCIAL ANALYTICS & REPORTING
-------------------------------

4. get_net_worth ⭐ ACTIVE
   - Calculates comprehensive net worth including all assets and liabilities
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(liquid_assets numeric, investments_total numeric, total_liabilities numeric, net_worth numeric, bank_accounts jsonb)
   - Purpose: Complete financial position summary
   - Used in: store_accounts.js

5. get_investment_snapshot ⭐ ACTIVE
   - Returns investment totals and cash balances
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(investments_total numeric, investment_cash numeric, as_of timestamp with time zone)
   - Purpose: Quick investment portfolio summary
   - Used in: store_accounts.js

6. get_recent_transactions ⭐ ACTIVE
   - Returns recent transactions with merchant and category info
   - Parameters: p_user_id (uuid), p_limit (integer)
   - Returns: TABLE(date date, amount numeric, merchant text, category text, name text)
   - Purpose: Quick access to latest transaction data
   - Used in: store_accounts.js

7. get_spend_by_category ⭐ ACTIVE
   - Returns spending breakdown by category for a date range
   - Parameters: p_user_id (uuid), p_start (date), p_end (date)
   - Returns: TABLE(category text, total_spend numeric, txn_count integer)
   - Purpose: Category-based spending analysis
   - Used in: store_accounts.js

8. get_cashflow_monthly ⭐ ACTIVE
   - Returns monthly cashflow data (income, expenses, net)
   - Parameters: p_user_id (uuid), p_months (integer DEFAULT 6)
   - Returns: TABLE(month date, income numeric, expense numeric, net numeric)
   - Purpose: Provides monthly financial overview for dashboard
   - Used in: store_accounts.js

9. get_spend_by_merchant ⭐ ACTIVE (STANDARD)
   - Merchant spending summary with optional merchant filter
   - Parameters: p_user_id (uuid), p_start (date), p_end (date), p_merchant (text DEFAULT NULL)
   - Returns: TABLE(merchant text, total_spend numeric, txn_count integer)
   - Purpose: Merchant-level totals; prefer this for summaries
   - Used in: analysis/reporting

10. get_transactions_by_category ⭐ ACTIVE
    - Returns transactions filtered by category
    - Parameters: p_user_id (uuid), p_category (text), p_start (date), p_end (date)
    - Returns: TABLE(id uuid, amount numeric, date date, name text, merchant_name text, category text, top_category text, sub_category text, transaction_type text)
    - Purpose: Category-filtered transaction listing
    - Used in: finny.js

11. get_spend_summary ⭐ ACTIVE (NEW)
    - Total spend and expense count for a date range
    - Parameters: p_user_id (uuid), p_start (date), p_end (date)
    - Returns: TABLE(total_spend numeric, txn_count integer)
    - Purpose: Fast answer to period-only spend queries (e.g., last month)
    - Used in: finny.js (time-period queries)

12. get_spend_by_month ⭐ ACTIVE (NEW)
    - Monthly total spend for last N months
    - Parameters: p_user_id (uuid), p_months (integer DEFAULT 3)
    - Returns: TABLE(month date, total_spend numeric, txn_count integer)
    - Purpose: Trend and multi-month questions
    - Used in: finny.js (multi-month queries)

13. get_spend_by_category_periods ⭐ ACTIVE (NEW)
    - Per-category monthly spend for last N months
    - Parameters: p_user_id (uuid), p_months (integer DEFAULT 3)
    - Returns: TABLE(month date, category text, total_spend numeric, txn_count integer)
    - Purpose: Category trends across months
    - Used in: finny.js (category trends)

GOALS & INVESTMENT TRACKING
---------------------------

11. get_goals_overview ⭐ ACTIVE
   - Returns active goals with progress percentage
   - Parameters: p_user_id (uuid), p_limit (integer DEFAULT 5)
   - Returns: TABLE(label text, category text, status text, target_amount numeric, current_amount numeric, progress_pct numeric, target_date date)
   - Purpose: Financial goals tracking and progress monitoring
   - Used in: store_accounts.js

12. get_investment_portfolio_summary ⭐ ACTIVE
   - Returns comprehensive investment portfolio summary
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(total_portfolio_value numeric, total_cash numeric, total_investments numeric, day_change numeric, day_change_percent numeric, total_change numeric, total_change_percent numeric, last_updated timestamp with time zone)
   - Purpose: Complete investment portfolio overview with performance metrics
   - Used in: investment components

13. get_investment_holdings_detailed ⭐ ACTIVE
   - Returns detailed investment holdings with performance data
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(symbol text, description text, units numeric, price numeric, market_value numeric, unrealized_pl numeric, day_change numeric, day_change_percent numeric, security_type text, last_updated timestamp with time zone)
   - Purpose: Comprehensive investment holdings analysis with performance tracking
   - Used in: store_accounts.js, investment components

14. get_investment_balances_summary ⭐ ACTIVE
   - Returns investment account balances and performance
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(cash numeric, buying_power numeric, total_value numeric, day_change numeric, day_change_percent numeric, total_change numeric, total_change_percent numeric, currency_code text, last_updated timestamp with time zone)
   - Purpose: Investment account balance and performance summary
   - Used in: investment components

15. get_investment_connections ⭐ ACTIVE
   - Returns active investment account connections
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(account_id text, brokerage_name text, account_name text, account_type text, is_active boolean, last_synced_at timestamp with time zone, connection_status text)
   - Purpose: Manage investment account connections and status
   - Used in: investment components

RECURRING TRANSACTIONS
----------------------

16. get_recurring_streams_active ⭐ ACTIVE
   - Returns active recurring income/expense streams
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(stream_id text, stream_type text, flow_type text, merchant_name text, category text, average_amount numeric, last_amount numeric, frequency text, first_date date, last_date date)
   - Purpose: Manages recurring transaction patterns
   - Used in: store_accounts.js

17. get_recurring_next_dates ⭐ ACTIVE
   - Calculates next occurrence dates for recurring streams
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(stream_id text, stream_type text, flow_type text, merchant_name text, category text, average_amount numeric, frequency text, last_date date, first_date date, next_date date)
   - Purpose: Predicts upcoming recurring transactions
   - Used in: store_accounts.js

ACCOUNT MANAGEMENT
------------------

18. delete_account_and_related_data ⭐ ACTIVE
   - Deletes a Plaid account and all associated data (transactions, recurring streams)
   - Parameters: p_account_id (text), p_user_id (uuid)
   - Returns: json { should_delete_item: boolean, item_id: text, remaining_accounts: integer, deleted_account_name: text, deleted_account_mask: text }
   - Purpose: Complete account deletion with cascade cleanup, returns metadata for Plaid item removal if needed
   - Used in: plaid_management.js (remove_account mode), AccountDetailModal.tsx
   - Notes: 
     * Verifies account ownership before deletion
     * Deletes all transactions and recurring streams for the account
     * Returns should_delete_item=true if this was the last account for its item_id
     * Caller should use returned metadata to determine if Plaid /item/remove API call is needed

SECURITY & CREDENTIAL MANAGEMENT
--------------------------------

19. secure_get_plaid_token ⭐ ACTIVE
   - Securely retrieves Plaid access token from Vault
   - Parameters: p_item_id (text), p_user_id (uuid)
   - Returns: text
   - Purpose: Secure access to encrypted Plaid tokens
   - Used in: transactions_sync.js, plaid.js, refresh_data.js, store_accounts.js, plaid_management.js, refresh_recurring_transactions.js, sync-transactions function

20. secure_store_plaid_token ⭐ ACTIVE
   - Securely stores Plaid access token in Vault
   - Parameters: p_item_id (text), p_user_id (uuid), p_token (text)
   - Returns: uuid (secret_id)
   - Purpose: Encrypted storage of banking credentials
   - Used in: store-plaid-token function

21. secure_delete_plaid_token ⭐ ACTIVE
   - Securely deletes Plaid access token from Vault
   - Parameters: p_item_id (text), p_user_id (uuid)
   - Returns: void
   - Purpose: Removes encrypted Plaid tokens when accounts/items are deleted
   - Used in: plaid_management.js (remove_item and remove_account modes)

22. secure_store_snaptrade_credentials ⭐ ACTIVE
   - Securely stores SnapTrade credentials in Vault
   - Parameters: p_user_id (uuid), p_snaptrade_user_id (text), p_account_id (text), p_user_secret (text)
   - Returns: uuid (secret_id)
   - Purpose: Encrypted storage of investment credentials
   - Used in: store-snaptrade-credentials function

AUTOMATIC CATEGORIZATION FUNCTIONS
----------------------------------

23. apply_category_rules_to_existing_transactions ⭐ ACTIVE
   - Applies category rules to existing transactions that don't have new_category set
   - Parameters: none
   - Returns: integer (updated_count)
   - Purpose: Bulk apply existing rules to uncategorized transactions
   - Used in: Automatic categorization system

24. auto_apply_category_rules ⭐ ACTIVE (TRIGGER)
   - Trigger function that automatically applies category rules to new transactions
   - Parameters: none (trigger function)
   - Returns: trigger
   - Purpose: Automatic categorization of new transactions based on existing rules
   - Used in: Automatic categorization system

UTILITY & TRIGGER FUNCTIONS
---------------------------

25. update_updated_at_column ⭐ ACTIVE
   - Trigger function to automatically update updated_at timestamp
   - Parameters: none (trigger function)
   - Returns: trigger
   - Purpose: Maintains audit trail for record modifications

26. update_web_scrape_cache_updated_at ⭐ ACTIVE
   - Trigger function to update web scrape cache timestamp
   - Parameters: none (trigger function)
   - Returns: trigger
   - Purpose: Cache invalidation for web scraping data

DEPRECATED/TO BE REMOVED
========================

get_spending_by_merchant (removed!)
 - Legacy detailed variant returning transactions jsonb.
 - Prefer `get_spend_by_merchant` for totals. If detailed list is needed, consider adding `get_transactions_by_merchant`.

CHAT HISTORY MANAGEMENT
------------------------

27. get_user_chat_sessions ⭐ ACTIVE
   - Returns user's last 5 chat sessions with metadata
   - Parameters: p_user_id (uuid)
   - Returns: TABLE(id uuid, session_title text, first_message text, created_at timestamptz, message_count integer)
   - Purpose: Display chat history in UI with session previews and counts
   - Used in: ChatHistoryScreen.tsx

28. save_chat_session ⭐ ACTIVE
   - Saves chat session and auto-cleanup (keeps only last 5 per user)
   - Parameters: p_user_id (uuid), p_session_title (text), p_first_message (text), p_messages (jsonb)
   - Returns: uuid (new session ID)
   - Purpose: Store complete conversations with automatic cleanup to prevent database bloat
   - Used in: useChat.ts (saveCurrentSession function)

29. get_chat_session_messages ⭐ ACTIVE
   - Returns full chat messages for a specific session
   - Parameters: p_session_id (uuid), p_user_id (uuid)
   - Returns: jsonb (array of chat messages)
   - Purpose: Restore complete conversation when user selects from history
   - Used in: useChat.ts (loadSession function)

ONBOARDING ANALYTICS
--------------------

30. log_onboarding_event ⭐ ACTIVE
   - Logs onboarding analytics events for tracking user progress
   - Parameters: p_stage (text), p_action (text), p_duration_ms (integer), p_error_code (text), p_metadata (jsonb)
   - Returns: void
   - Purpose: Track user behavior during onboarding (screen views, completions, Plaid outcomes, drop-offs)
   - Used in: welcome.tsx, intent.tsx, aboutyou.tsx, accountconnection.tsx, final.tsx
   - Notes:
     * Automatically captures user_id from auth.uid()
     * Stores in onboarding_events table with RLS
     * Used for analytics, A/B testing, and optimization
     * Common actions: 'view', 'complete', 'skip', 'error', 'plaid_success', 'plaid_error'

TOTAL: 34 ACTIVE functions, 0 REMAINING unused functions

NOTES:
- ⭐ ACTIVE functions are currently used in the codebase
- All functions use Row Level Security (RLS) where applicable
- Functions with p_user_id parameter automatically filter by authenticated user
- Date parameters use PostgreSQL date type for consistency
- Numeric parameters use PostgreSQL numeric type for precision
- Functions return TABLE types for complex result sets
- Security functions use Supabase Vault for encrypted credential storage
- Automatic categorization functions work with triggers for real-time categorization
- Chat history functions use JSONB for efficient message storage and retrieval
